{
  "name": "personalsite",
  "homepage": "https://YOUR_GITHUB_USERNAME.github.io/REPO_NAME",
  "version": "1.0.0",
  "description": "1. Make a simple landing page 2. Make a template for a blog post 3. Make a markdown to HTML converter 4. Add a contact form",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node scripts/build.js",
    "start": "http-server dist",
    "deploy": "npm run build && gh-pages -d dist",
    "dev": "npm run build && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "marked": "^15.0.6"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "http-server": "^14.1.1"
  }
}
