{
  "name": "personalsite",
  "version": "1.0.0",
  "description": "1. Make a simple landing page 2. Make a template for a blog post 3. Make a markdown to HTML converter 4. Add a contact form",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node scripts/build.js",
    "start": "http-server ./dist -p 3000",
    "deploy": "npm run build && echo 'Please specify your deployment commands here'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "marked": "^15.0.6"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
